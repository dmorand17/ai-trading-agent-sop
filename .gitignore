# Secrets — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key

# Kill switch — local-only operational flag, not content
KILL_SWITCH

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
*.swo
__pycache__/
.python-version
.venv/
node_modules/

# Logs and caches
*.log
.cache/

# SOP audit artifacts — ephemeral, gitignored per risk-review.md §3-4
proposals/
reviews/
